name: Lupo Studios Quality Gate (disabled)

# DISABLED 2026-04-13: These checks run locally via Claude Code hooks (quality-gate.sh).
# CI enforcement was generating hundreds of failure emails due to overly strict regex.
# Re-enable after fixing the .gitignore check regex and adding real build/test steps.
on:
  workflow_dispatch:

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Quality gate is currently disabled. Checks run locally via Claude Code hooks."
